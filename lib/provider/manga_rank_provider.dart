import 'package:flutter/material.dart';

/*

class MangaProvider extends ChangeNotifier {
  List<Manga> MangasCollection = <Manga>[];

  void updateMangaRanking(List Mangas) {
    for (var Manga in Mangas) {
      MangasCollection.add(Manga.fromJson(Manga));
    }
    notifyListeners();
  }
}

*/